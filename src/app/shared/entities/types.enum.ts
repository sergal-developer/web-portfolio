export enum TypeScope {
    DESIGN = 'design',
    ADMIN = 'admin',
    BACKEND = 'backend',
    FRONTEND = 'frontend',
    MOBILE = 'mobile',
}

export enum TypeSkill {
    SOFT = 'soft',
    HARD = 'hard',
    COURSE = 'course',
}

export enum TypeLevel {
    TRAINEE = 0,
    JUNIOR = 1,
    ADVANCED = 2,
    SENIOR = 3,
    EXPERT = 4,
}
